function SensMessage() {
    return (
        <p>If you are interested in our offer then Send a message by clicking on <a href="https://mail.google.com/">Send message</a></p>
    )
}

export default SensMessage;